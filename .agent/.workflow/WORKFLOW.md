# Agent Workflow

1. See `PLAN.md`(create or overwrite for specific task if needed)
2. Create `Task.md` for easy walkthrough
3. Update `Task.md` if finish
4. Review and test for each task
5. Update review in `CHANGELOG.md`(create if missing)